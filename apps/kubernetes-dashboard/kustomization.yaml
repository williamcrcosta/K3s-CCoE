apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - rbac.yaml
  - deploy.yaml
  - svc.yaml
  - ingress.yaml
  # - ingressroute.yaml  # mantido como historico; substituido por ingress.yaml no RKE2
